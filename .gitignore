node_modules/
.env

# SQLite content store (runtime data — lives on a Railway volume in production)
data/*.db
data/*.db-wal
data/*.db-shm

# Cloud-sync (OneDrive) name-clash artifacts — never commit these
*# Name clash *#*
